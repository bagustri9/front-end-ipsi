const judulFungsi = (props) => {
    return (
        <div>
            <h1 className="h3 mb-0 text-gray-800">{props.title}</h1>
            <p className="fs-6 text-muted mt-0">{props.info}</p>
        </div>
    )
}

export default judulFungsi